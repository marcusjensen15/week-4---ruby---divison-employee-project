<h1></h1>

<% if @divisions.any? %>
<ul>
  <% @divisions.each do |division| %>
  <li><%= link_to division.name, division_path(division) %></li>
  <% end %>
</ul>
<% else %>
<p>There are no divisions yet.</p>
<% end %>


<p><%= link_to "Create new division", new_division_path %></p>

<% if @employees.any? %>
<ul>
  <% @employees.each do |employee| %>
  <li><%= link_to employee.employee_name, employee_path(employee) %></li>
  <% end %>
</ul>
<% else %>
<p>There are no employees yet.</p>
<% end %>


<p><%= link_to "Create new employee", new_employee_path %></p>

<% if @projects.any? %>
<ul>
  <% @projects.each do |project| %>
  <li><%= link_to project.project_name, project_path(project) %></li>
  <% end %>
</ul>
<% else %>
<p>There are no projects yet.</p>
<% end %>


<p><%= link_to "Create new project", new_project_path %></p>
